import 'package:flutter_test/flutter_test.dart';
import 'package:mockito/mockito.dart';
import 'package:perfect_corp_homework/features/download/repository/download_repository_impl.dart';

void main() {
  setUp(() {});
  tearDown(() {});

  /// given when then
  test('GIVEN download repository implementation class WHEN the THEN', () {
    final DownloadRepositoryImpl downloadRepositoryImpl =
        DownloadRepositoryImpl();
  });
}
